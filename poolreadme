这个是图库程序

TestGameId = 137   改成多少就跑多少

imageType  改成几就跑 几的图库   改成0就是自然跑  大象1是普通  2是免费

isGzip = 0  是是否压缩  不压缩可以在数据库直接查看  但是服务器只能加载压缩后的数据

ischecked  是 是否检查图库各个倍数类型 生成的个数  false是不检查


.\go_build_runImage.exe .\imagePoolInit.lua  开始跑图库

mongo.json  这是数据库位置

pool_gameid    是图库数据库表


可以在json.cn  上检查有没有逻辑 错误  和 客户端要的大概  数据格式

ok没？

isGzip = 0  是是否压缩  不压缩可以在数据库直接查看  但是服务器只能加载压缩后的数据

这个是不是要设置1服务器那边才能加载
对
而且 服务器加载的时候 要把未压缩的数据 删掉
就是还有重新跑一次吗
对的  设置成0只是看数据结构  和有没有BUG
hao 
imageType  改成几就跑 几的图库   改成0就是自然跑  大象1是普通  2是免费
一般就是0就可以了 
对的，，但是有些时候  需要专门跑一些特殊模式下的图库 liaojie 
那我退下了  你先看下   和海东一起看下  他以后要负责这一块 hao 